{
  "name": "pyjs-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root orchestrator for pyjs cross-language bridge",
  "scripts": {
    "ts:install": "npm install --prefix typescript",
    "ts:demo": "npm run demo --prefix typescript",
    "ts:build": "npm run build --prefix typescript"
  }
}
